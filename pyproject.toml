[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "shutterstock_analysis"
description = "Analyze Shutterstock insights for photo licensing"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.7"
dependencies = []